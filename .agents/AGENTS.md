# Project Rules

- **Native Dialogs**: NEVER use native browser dialogs such as window.alert, window.confirm, or window.prompt. Always build and render custom in-page UI modal dialogs using React state.
